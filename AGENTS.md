# 项目协作规则

## 人类摘要

| 主题 | 项目规则 | 详情 |
| --- | --- | --- |
| 范围 | 本目录及其子目录；当前处于需求与设计文档阶段 | [范围与优先级](#scope) |
| 基线 | 当前不是 Git 工作区，未发现集成分支、远端或保护规则 | [Git 与 GitHub](#git-workflow) |
| PR | 不把本次文档任务当作推送、合并或发布授权；区分人类决定与 agent 推断 | [Git 与 GitHub](#git-workflow) |
| 设计 | 独立的可信场景是产品基线，PRD 从中派生，其他历史故事只作参考 | [设计治理](#design) |
| 技能 | 使用实际可读且匹配任务的技能；本文件按已安装的生成技能编写 | [技能路由](#skills) |
| 参考 | 未选定产品实现参考仓库或技术栈，不从参考项目推定产品需求 | [参考与调研](#references) |
| 研究 | 比较模型、提示词或算法时保留可复现的版本树；当前不建空目录 | [研究记录](#research) |
| 验收 | 验证用户目标与关键状态；文档通过检查不等于产品功能已实现 | [充分验收](#acceptance) |

## 待人类确定的事项

- **Git / GitHub 治理**：仓库地址、集成基线、保护分支、PR 目标和合并方式尚无证据。本次只编辑本地文件，不自动初始化 Git、配置远端或选择 `main` / `develop`。
- **技术与运行方案**：当前没有应用代码、依赖清单、CI 或构建测试入口。后续实现先按当时已授权范围核实方案，不因安装技能使用过 `npx` 就认定应用采用 Node.js。
- **产品参数**：推荐与热度算法、文件限制、投票与验证阈值、额度等未由可信场景定值。文档中标为待定；实现影响公开行为的参数前取得明确依据，不为了填满 PRD 自行补值。

以上未知事项不阻塞本轮已经授权的技能安装、`AGENTS.md` 编写、可信场景提取和 PRD 整理，也不要求对相同授权重复确认。

<a id="scope"></a>
## 范围与优先级

- 本文件适用于项目根目录及其所有子目录的 agent 工作。更近目录的有效 `AGENTS.md` 仅在其声明的范围内覆盖本文件；开始修改前检查指令链。
- 保留与任务无关的用户修改。当前用户指定的交付物是项目指令、提取的可信设计和 PRD，不包含应用实现、云端环境搭建或发布。
- 在系统与开发者指令允许的范围内，冲突依次按以下依据处理：当前明确的人类指令 → 有效 `AGENTS.md` 链 → 已明确授权的可信设计 → 已确认的任务 / Issue / PR 契约 → 既有兼容性测试 → 当前实现 → 外部参考。
- 人类后续修正覆盖 agent 推断。发现会影响用户行为的冲突时明确指出并解决，不能为了沿用现有实现而静默修改需求。
- 使用中文文档与沟通，保留工具名、协议名及必要代码标识的原文。项目指令采用可被工具识别的标准文件名 `AGENTS.md`。

<a id="expectations"></a>
## 用户期望与授权

每轮持续维护目标、可观察结果、范围、排除项、产品不变量、验收证据、已有授权、偏好、假设和未决事项；无需为此额外创建文件。用户在工作中的补充优先作为当前任务修正处理。

- **Human Design**：只有用户明确提出、明确批准或明确指定为可信的内容。当前用户指定的可信范围是端到端交互场景，不是整份历史用户故事。
- **Agent Self-Claimed**：agent 自行引入的路径、编号、文档组织、技术方案、命名、依赖、测试组织及从参考项目得到的设计。不得把这些改写为“用户已确认”。
- 沉默不代表批准。可逆且不改变产品语义的编辑、排版与文件组织可以继续，并在交付时说明；影响公开行为、API、架构、依赖、数据、安全、迁移或显著成本的未授权决定应先澄清。
- 既有明确授权持续有效。本轮用户已授权执行指定的全局技能安装命令及三个文档交付方向；不要从技能中的一般审批要求推导出对这些动作的重复审批。
- 输出交付物时对照目标说明实际完成、验证范围、推断和剩余问题；不把设计文档描述成已运行的功能。

<a id="git-workflow"></a>
## Git 与 GitHub

### 已检查的事实与分支规则

2026-09-17 检查时，`git rev-parse --show-toplevel` 表明当前目录不在 Git 工作区；因此没有可验证的远端、默认分支、跟踪分支、合并历史、PR 模板或 CI 门槛。集成基线、发布分支、受保护分支和默认 PR 目标均未确定。

- 后续出现 Git 仓库时，在修改前检查 `git status --short`、当前分支、远端、跟踪关系、近期合并历史和实际贡献约定。远端默认分支不自动等于集成基线。
- 未确定基线前不自动创建或切换开发分支，不合并、不 rebase、不 cherry-pick 到猜测的目标。
- 基线确定后，新任务从当前已核实的基线创建分支；基线到任务分支的同步，以及任务分支到集成 / 发布分支的流入，分别遵循已确认的合并策略。未确认 merge、rebase、squash 或 cherry-pick 方式时不改写已有历史。
- 推送、创建 / 更新 PR、合并、发布分别按会话中已有授权执行；不得把其中一种授权扩大成其他动作，也不得对已明确授权的同一步骤再次索要许可。

### 临时分支约定

尚未发现既有命名规则。若后续任务已授权分支工作且基线已确定，建议采用 `codex/<type>-<short-topic>`，使用小写、描述性名称，一条分支对应一个任务和一个 PR。这是 **Agent Self-Claimed** 的最小建议，既有项目规则或人类指令优先。

分支不夹带无关清理；有脏工作区时先保留原修改，必要时创建隔离 worktree。同步方式遵循已确认策略；仅在合并完成或用户明确放弃后删除任务分支。

### PR 作者归属

PR 正文以问题及最终行为开头，并使用以下小节：

```markdown
## Human Design
- 只列用户明确提出或批准的决定。

## Agent Self-Claimed
- 列 agent 自行引入的实现、结构、依赖、测试和文档决定；没有则写 None。

## Validation
- 列实际命令、退出码、结果、证据和未完成的必要验证。
```

归属不明确的决定放入 Agent Self-Claimed；不以 `misc`、`chore` 或“顺便”掩盖新增范围。

<a id="design"></a>
## 设计治理

### 依据与可信范围

当前产品基线为 [可信设计：端到端交互场景](docs/design/trusted-end-to-end-scenarios.md)。用户明确指定该模块为可信设计，文中六个场景原样提取自 [用户故事文档](docs/user-stories.md) v0.4 的第 4 节。

[PRD](docs/prd.md) 是依据六个场景编排的派生需求文档，使用 S1～S6 追溯来源，并把未明确的细则列为待定；不能把 PRD 的组织方式或待定项说成新的人类批准。原用户故事文件继续保留作为历史参考，其余章节不自动约束新实现。

可信场景提取应保留原文及来源信息。场景中的历史故事编号只用于追溯，不能借该编号把原文其他章节的细则自动提升为已确认需求。PRD 应为每项功能及验收条件标注对应场景，新增组织方式、编号和未决问题属于文档整理，不是新增产品批准。

| 范围 | 治理依据 | 状态 | 使用时机 |
| --- | --- | --- | --- |
| 产品流程与 UI | 可信设计 S1～S6；派生 PRD 第 4、6 节 | 场景由人类明确指定为可信；PRD 不扩大行为范围 | 首页发现、投稿、管理员 Dashboard、统计查看、看板排行变更 |
| 架构与模块 | 可信设计中的角色边界与交接 | 行为边界可信；尚无技术架构治理文档 | 云端交互、模块职责或调用方向变化 |
| 公开 API / MCP | 可信设计 S3、S5；派生 PRD FR-05 | 读取用途、token 身份及统计副作用明确；请求结构未定 | MCP 能力、统计访问或投稿入口变化 |
| 数据 | 可信设计 S2～S5；派生 PRD 第 5 节 | 发布状态与账号 × 任务资格明确；存储设计未定 | 投稿、审核、资格持久化、重试与运行记录变化 |
| 安全与权限 | 可信设计 S3～S5；派生 PRD 第 3 节 | 管理员权限及作品授权边界明确；尚无详细安全方案 | 身份、材料授权、管理操作或资格检查变化 |
| 测试与验收 | 可信设计、派生 PRD 第 7 节及本文件 | 场景可作为判断依据；无现成测试框架 | 文档核对与后续功能实现 |
| 研究 | 本文件研究记录规则 | agent 提出的工作约定；无现成研究制品 | 模型、提示词、验证或排序算法实验 |
| 发布与 GitHub | 尚未发现治理文档 | 待确定 | 推送、合并、部署或发布前 |

### 必须保持的产品边界

1. 首页直接进入平台推荐的无限发现流；看板与按热度排序的排行榜同页。
2. 新任务至少带一件人类和一件 agent 作品，审核后才公开；所有用户均可向任何可见任务追加一方作品，无需任务作者批准。
3. 任务可见即已过审，追加不重审原任务；新增作品和云端产出各自经过发布审核。
4. 管理员 Dashboard 与公开看板分开；只有平台管理员能启动、取消、重跑和配置云端挑战。
5. 运行终止后的迟到结果不继续推进或登记作品；登记重试先找回已有结果，避免重复投稿或绕过审核。
6. 普通读取不预取具体投票统计。显式查看统计先永久关闭该账号在该任务上的后续投票资格，再返回统计；网站与 MCP 共用记录。
7. 换 token、换作品对或新增作品不恢复该题投票资格，其他任务不受影响；失去投票权不妨碍追加作品。
8. MCP 只承担内容读取，不代为投稿或投票，也不控制云端；显式统计读取必须披露永久资格变化这一副作用。

未由可信场景定义的参数、方案和完整 API 契约不得伪装成已批准设计。需要新增治理文档时先检查现有授权；本轮提取可信设计和编写 PRD 已在授权范围内。

<a id="skills"></a>
## 技能路由

| 任务 | 已验证技能 | 触发条件 | 读取位置 |
| --- | --- | --- | --- |
| 创建 / 更新项目 AGENTS.md | codex-agents-md-generator | 用户要求初始化或修订项目 agent 指令 | [SKILL.md](/home/oops/.agents/skills/codex-agents-md-generator/SKILL.md)，以及其 generation-contract.md |
| 安装指定技能 | skill-installer | 用户要求安装技能或提供安装命令 | [SKILL.md](/home/oops/.codex/skills/.system/skill-installer/SKILL.md) |

- 每次按任务检查当前可用技能，选择最小充分集合，向用户说明所用技能与原因；完整读取选中技能及其必需引用，不能仅凭名字推断要求。
- 用户点名的技能优先使用；不可用时先搜索并说明。用户明确指令高于冲突的技能建议，交付时说明实质性偏离。
- 专门的鉴权、安全、数据库、事务、并发、一致性、性能、协议、UI / 无障碍、CI/CD、供应链或研究任务若无已覆盖技能，先搜索适用技能；未发现时依据官方规范和上游证据推进，并说明能力缺口。
- 安装或扩展依赖、权限先判断会话中是否已有授权；没有时才请求。不要为普通文档整理批量安装无关技能。
- 本轮已按用户命令安装 generator。安装记录位于 `/home/oops/.agents/.skill-lock.json`，来源为 `KDZZZZZZ/codex-agents-md-generator`；安装目录摘要是 `235448749c077e373af59aa7bd2b7666b09f6ca0`，该值不是 Git commit 证明。

<a id="references"></a>
## 参考与调研

目前未选定框架、数据库、模型 SDK 或产品实现对标仓库，不建立通用技术清单。唯一已核实的工具参考是 [AGENTS 生成技能仓库](https://github.com/KDZZZZZZ/codex-agents-md-generator)，对应本地已读的 `SKILL.md` 与 `references/generation-contract.md`，用途限于项目指令生成。

调查顺序：会话与期望模型 → 本地可信设计 → 测试及公开接口 → 实现与 Git 历史 → 项目明确配置的参考仓库 → 官方规范与上游实现 → 成熟实现。

- 产品意图不明确时向人类确认；实现选择可通过证据研究；证据不足时重建验收矩阵。时效性事实使用当前权威来源核实。
- 新协议 / 框架、冲突资料、高风险恢复与一致性、版本变化或仅有概念演示时，补充适用参考，不把“能够实现”当作“适合本项目”。
- 记录具体文件、版本或 commit、相关测试、行为差异和取舍。优先规范、官方文档、上游代码与测试，其次成熟实现、论文和基准；博客与论坛仅作线索。
- 本地人类要求和可信设计优先；从参考项目引入的行为仍属 Agent Self-Claimed。不能访问来源时如实说明。

<a id="research"></a>
## 研究记录

当前没有实验代码或结果目录。若后续开展模型、提示词、偏好验证、推荐 / 热度算法或其他方案比较，保存逻辑版本树，例如 `R0 基线 → H1 假设 → V1 / V2 实验 → S1 综合结论`，不要求每个节点对应长期 Git 分支。

每个重要节点记录：唯一 ID 与父节点、问题 / 假设 / 相对变化、数据和依赖 / 模型 / 参数 / 环境版本、可恢复代码或补丁 / commit / 制品、准确命令与条件、原始结果位置、证据结论、局限及后续分支；状态使用 `candidate`、`accepted`、`rejected`、`inconclusive` 或 `superseded`。

实验必须从明确父节点派生；保留能排除假设的失败，避免同时改变多个关键变量，只比较条件兼容的结果。代码与原始结果保存在聊天外，大制品记录哈希与取回方式；不保存密钥或未经授权的作品正文。新结果取代旧结论时保留历史，综合节点说明选中与排除的分支。

尚无既定制品位置，可在真实研究任务中采用 `research/<topic>/` 下的说明、版本树、代码、结果和制品目录。这是 Agent Self-Claimed 的默认建议；本轮不创建空研究目录。

<a id="acceptance"></a>
## 充分验收

**验收通过应使人对用户目的已经达成有较高信心，不能只证明能编译或单个样例能运行。**

每项工作建立与风险匹配的矩阵：

| 用户目标 | 前置条件 | 输入 / 动作 | 可观察结果 | 所需证据 | 失败含义 |
| --- | --- | --- | --- | --- | --- |
| 文档忠实于场景 | 已明确可信范围 | 提取、整理、核对 | 原文不失真，需求可追溯，无暗增行为 | 原文比对、链接和编号检查 | 需求污染或遗漏 |
| 发布与开放投稿 | 有任务 / 作品状态和不同账号 | 双方首发、任意用户追加 | 缺一方不送审；追加不重审任务；新作品独立审核 | 后续真实入口及状态记录 | 权限或发布流程错误 |
| 统计与永久资格 | 账号已识别，网站和 MCP 可访问 | 显式看统计，再尝试投票 | 先落资格变化，再返回结果；换 token / 作品对仍不可投 | 后续跨入口及持久化证据 | 统计泄露或资格可绕过 |
| 管理员独占云端 | 有普通用户与管理员 | 启动、取消、重跑和登记重试 | 权限正确，终止后迟到结果无效，作品不重复 | 后续权限、恢复和真实交互证据 | 未授权执行或重复作品 |

- 从人类要求、可信设计、官方规范或已有兼容契约取得判定依据，不以当前实现自证正确。
- 覆盖适用的正常路径、边界、非法输入、否定行为、状态持久化、并发与恢复、接口和用户可见结果、相邻回归，以及安全、性能和额度限制。只运行与本次变更相关且必要的检查。
- 缺陷修复的证据必须能识别旧缺陷；必要的真实集成不能以只验证内部调用的 mock 替代。纯文档变更不强制新增软件测试或安装检查工具。

### 当前可用的检查与缺失入口

当前只有 Markdown 文档；未发现构建、lint、单元、集成、端到端、迁移或发布脚本。不得虚构 `npm test`、CI 已通过或产品已验收。已验证可用工具为 `rg`、`python3`、`git`、`npx`，后两者存在不代表项目已采用其工作流。

当前文档工作使用 `rg --files --hidden -g '!.git' -g '!node_modules'` 检查文件，用 `python3` 检查文件存在、提取文本一致、故事 / 需求编号、Markdown 本地链接与锚点。命令及结果在交付时按实际运行报告。

### 结论门槛

标记 PASS 前回答：是否仍有现实路径绕过用户目标；是否经过真实入口和关键组件；判据是否独立；证据是否能发现目标缺陷；是否覆盖相关条件而非单例；是否检查可见结果；是否覆盖相邻回归；环境是否有代表性；mock 是否可能掩盖集成失败；研究能否复现。

- **PASS**：准确命令、退出码、结果与必要制品支持声明范围内的关键目标。
- **FAIL**：指明不满足的要求、证据及影响。
- **INCONCLUSIVE**：必要门槛无法验证且替代证据不足时，列缺口、已尝试方案和重跑条件，不声称通过。

本轮可以对文档交付作结论；没有应用实现时不能对未来功能给出 PASS。研究还需预先明确问题与指标，保存同条件基线、原始结果及可恢复代码，并让结论受证据边界约束。
